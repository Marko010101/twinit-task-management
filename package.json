{
  "name": "my-workspace",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors blue,green \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "test": "concurrently --names backend,frontend --prefix-colors blue,green --group \"npm test --workspace=backend\" \"npm run test:run --workspace=frontend\"",
    "test:backend": "npm test --workspace=backend",
    "test:frontend": "npm run test:run --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
